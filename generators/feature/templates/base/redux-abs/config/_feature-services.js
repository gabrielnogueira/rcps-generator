import {Api} from 'services';

const <%=FEATURE_NAME%>Service = {
        //EXAMPLE =>

        // getByIngredients: function(ingredients, page){
        //     const params = [
        //         `i=${encodeURIComponent(ingredients)}`,
        //         `p=${page}`
        //     ].join('&');
        //     return Api.get(`/api/?${params}`);
        // }
}

export default <%=FEATURE_NAME%>Service;
