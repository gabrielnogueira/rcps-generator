import {STATE_NAME, reducers, logic} from './redux-abs';

export {default as <%=FEATURE_NAME%>} from './<%=FEATURE_NAME%>'

export const <%=FEATURE_NAME_LOWER_CASE%> = {
    reducers,
    logic,
    STATE_NAME
}